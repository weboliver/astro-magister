"""
Make the tests directory a package so tests can import helpers
using `from tests.support import ...` when pytest is run from the
project root or other working directories.

This file intentionally empty otherwise.
"""
